<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start"
    android:background="@drawable/background1">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to the Harry Potter Index!"
        android:paddingTop="63dp"
        android:paddingLeft="15dp"
        android:id="@+id/NavBarDescirption"
        android:shadowColor="#000000"
        android:shadowDx="1"
        android:shadowDy="1"
        android:shadowRadius="1.5"
        android:textColor="#B8860B"
        android:textSize="50sp"/>

    <TextView
        android:id="@+id/descriptionofNav"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="(To see categories swipe from the right to the left)"
        android:shadowColor="#000000"
        android:shadowDx="1"
        android:shadowDy="1"
        android:paddingTop="475dp"
        android:shadowRadius="1.5"
        android:textColor="#B8860B"
        android:textSize="35sp"/>

    <include
        layout="@layout/app_bar_nav_bar"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header_nav_bar"
        app:menu="@menu/activity_nav_bar_drawer" />

</android.support.v4.widget.DrawerLayout>
