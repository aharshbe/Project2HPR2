<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.austin.harrypotterrev2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Only allows one instance of this actvity -->
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".Main2Activity" />
        <activity android:name=".CoverPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".easteregg" />
        <activity android:name=".BooksActivity" />
        <activity android:name=".bookDescription" />
        <activity android:name=".bookDescription2" />
        <activity android:name=".bookDescription3" />
        <activity android:name=".QuotesActivity" />
        <activity android:name=".quoteDescription" />
        <activity android:name=".dobbyDescription" />
        <activity android:name=".snapeDescription"></activity>
    </application>

</manifest>